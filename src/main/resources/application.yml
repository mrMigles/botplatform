server:
    address: 0.0.0.0
    port: ${PORT}

spring:
  data:
    mongodb:
      database: ${DATABASE}
      password: ${PASSWORD}
      host: ${MONGO_HOST}
      port: ${MONGO_PORT}
      username: ${USERNAME}